#pragma once
#include "Core/Core.h"
#include "Networking/NetworkingCore.h"

/*
namespace Net
{
    struct Message
    {
        uint16  Size;
        std::vector<Byte> Data;
    };

    class MessageSocket
    {
        public:
        bool SendMessage(const void* data, uint16 size);
        bool ReceiveMessage(Message& out);
    private:
        Socket m_Socket;
    };

}
*/